<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.jvnet.hudson.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>1.138</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <name>Naginator</name>
    <description>
     Naginator is a plugin for Hudson that schedules a new build immediately following a build failure, 
     thereby nagging the project members that the build is failing.
    </description>
    <groupId>com.chikli.hudson.plugin.naginator</groupId>
    <artifactId>naginator</artifactId>
    <packaging>hpi</packaging>
    <version>1.0-SNAPSHOT</version>
</project>
